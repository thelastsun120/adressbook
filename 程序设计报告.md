# 通讯录管理系统程序设计报告（Qt + C++ + MySQL）

## 1. 课题背景与意义

在《C++面向对象程序设计》课程中，通讯录管理系统是一个非常典型且具有实践价值的综合训练题。其价值不仅在于实现“增删改查”这些基础功能，更在于能够把课堂中抽象的面向对象概念——如类、对象、封装、抽象、职责划分、模块协作——落地为可运行的软件系统。与此同时，随着课程作业难度提升，单纯的控制台程序已无法满足“结构化设计 + 用户体验 + 持久化数据管理”的综合要求，因此本项目采用 Qt Widgets 构建图形化界面，采用 MySQL 作为后端数据库，并配套文本初始化与同步机制，最终形成一个具备较完整工程形态的桌面应用原型。

本系统面向的核心场景是：用户通过图形界面对联系人信息进行录入、查询、分类、修改、删除和保存。相较于传统数组/链表内存管理方式，本系统将联系人信息持久化到数据库，在程序关闭后仍可保持数据；并通过 `config.ini` 外置连接参数，降低部署门槛；同时保留 `contacts.txt` 作为初始数据源与导出载体，实现教学中“文件 + 数据库”的融合练习。

因此，本课题在教学意义上有三点：
1. 训练面向对象建模能力（对象抽象、类职责、方法边界）；
2. 训练 GUI 事件驱动编程能力（信号槽机制、界面状态同步）；
3. 训练数据持久化与工程组织能力（SQL、配置化、构建脚本）。

---

## 2. 需求分析

### 2.1 功能需求

根据课程要求与系统实现，系统需完成如下功能：

1. **联系人信息管理**
   - 每位联系人至少包括：姓名、性别、电话、QQ、类别；
   - 类别可用于区分亲人、同学、朋友、同事等。

2. **通讯录基本操作**
   - 添加联系人；
   - 修改联系人；
   - 删除联系人；
   - 显示全部联系人。

3. **查询与筛选**
   - 按姓名查找；
   - 按电话查找；
   - 按类别过滤显示。

4. **持久化相关**
   - 启动时连接数据库并建表（若不存在）；
   - 数据库为空时，自动从 `contacts.txt` 导入初始数据；
   - 可将数据库数据同步导出回 `contacts.txt`。

5. **界面交互优化**
   - 提供清晰的输入区、查询区、操作区、数据显示区；
   - 提供状态提示与必要的确认弹窗（如删除确认）。

### 2.2 非功能需求

1. **易用性**：界面结构清晰、按钮含义明确、错误提示友好；
2. **可维护性**：代码结构模块化，方法职责明确；
3. **可扩展性**：后续可扩展字段（邮箱、地址、生日）与功能（排序、分页、权限）；
4. **可靠性**：数据库约束防止重复数据（如电话唯一）；
5. **可部署性**：通过 CMake 管理构建，配置文件管理连接参数。

---

## 3. 总体设计思路

本系统采用“界面层 + 业务控制层 + 数据访问层”的分层思想，虽然体量不大，但在职责上具备清晰边界。

1. **界面层（UI）**
   由 Qt Widgets 构建，负责展示输入控件、表格、按钮、提示信息，接收用户操作。

2. **业务控制层（MainWindow 槽函数）**
   负责业务流程编排：参数校验、调用 SQL、刷新表格、提示结果。

3. **数据访问层（SQL 语句封装）**
   通过 `QSqlDatabase` + `QSqlQuery` 与 MySQL 交互，完成建表、插入、更新、删除、查询。

4. **配置与初始化层**
   - 通过 `config.ini` 读取数据库连接参数；
   - 通过 `contacts.txt` 完成空库初始化和数据导出。

该设计的核心优点是：
- 结构直观，便于教学展示；
- 层间依赖简单，便于后续重构；
- 既能满足作业要求，又具备小型工程可运行能力。

---

## 4. 面向对象设计分析

### 4.1 对象抽象

系统将“联系人”抽象为 `Person` 数据结构，包含：
- `name`（姓名）
- `gender`（性别）
- `phone`（电话）
- `qq`（QQ）
- `category`（类别）

这一抽象体现了对象建模的“高内聚”原则：与联系人相关的数据集中在一个实体中管理，便于在查询结果、表格显示、表单收集时统一处理。

### 4.2 封装与职责划分

`MainWindow` 类作为主控制器，封装了系统主要行为：
- `setupUi()`：界面初始化；
- `loadDbConfig()`：读取配置；
- `initDatabase()`：数据库连接与建表；
- `loadFromTextIfDbEmpty()`：空库导入；
- `queryContacts()`：查询接口；
- `addContact()/modifyContact()/deleteContact()`：CRUD 核心；
- `saveContacts()`：导出文本；
- `refreshTable()`：界面刷新。

这种封装方式让每个方法语义单一、边界明确，便于调试与维护。

### 4.3 继承与多态说明

本项目中 `MainWindow` 继承自 `QMainWindow`，复用 Qt 已有窗口框架能力。虽然项目未显式实现自定义多态层次（如虚函数接口族），但 Qt 信号槽机制天然体现事件驱动分发思想，从应用结构角度仍具备“接口调用 -> 不同槽函数执行”行为分派特征。

### 4.4 面向对象优点在本项目中的体现

1. 业务逻辑聚合在类中，减少全局变量污染；
2. 功能扩展时只需在对应类增量修改；
3. UI 与数据逻辑通过方法边界连接，降低耦合；
4. 模块命名清晰，便于团队协作和代码审阅。

---

## 5. 数据库设计

### 5.1 连接方式

系统采用 Qt Sql 模块中的 `QMYSQL` 驱动连接 MySQL，连接参数来自 `config.ini`：
- host
- port
- name
- user
- password

这种配置化连接相较于硬编码更安全、灵活，方便在不同机器部署。

### 5.2 表结构设计

系统表 `contacts` 的核心字段如下：
- `id INT PRIMARY KEY AUTO_INCREMENT`
- `name VARCHAR(50) NOT NULL`
- `gender VARCHAR(10) NOT NULL`
- `phone VARCHAR(20) NOT NULL UNIQUE`
- `qq VARCHAR(20)`
- `category VARCHAR(50)`

设计要点：
1. `id` 作为主键便于精准修改/删除；
2. `phone` 设置唯一约束，避免重复联系人；
3. 采用 `utf8mb4` 保证中文兼容。

### 5.3 初始化策略

程序启动后先统计 `contacts` 记录数，若为 0，则读取 `contacts.txt` 逐条导入。该策略使系统“开箱即用”，同时保留课程作业中文件读写能力训练。

### 5.4 同步导出策略

`saveContacts()` 将数据库按 `id` 顺序导出为文本，格式：

`name|gender|phone|qq|category`

此机制可用于备份、迁移、演示，也便于课程验收时直观查看数据结果。

---

## 6. 界面设计与交互流程

### 6.1 布局设计

界面主要分为五个区域：
1. 标题区：显示系统名称；
2. 表单区：输入联系人信息；
3. 查询区：输入关键字并按条件检索；
4. 操作区：添加、修改、删除、显示全部、同步文本；
5. 表格区：展示联系人列表（含隐藏 ID 列）。

此外在底部增加状态标签，显示“当前记录数”，增强交互反馈。

### 6.2 关键交互设计

1. **按选中行修改/删除**
   - 用户必须先选中表格行；
   - 系统读取隐藏 ID 列进行数据库操作；
   - 避免同名联系人导致误修改。

2. **删除确认弹窗**
   - 删除前二次确认，防止误操作。

3. **输入合法性校验**
   - 添加/修改时要求姓名和电话不能为空；
   - 电话重复时由数据库约束返回失败提示。

4. **状态反馈**
   - 每次刷新后显示当前记录条数；
   - 异常通过消息框提示具体问题。

### 6.3 美化策略

通过样式表（QSS）设置按钮颜色、悬浮效果、表格交替行、边框圆角等，增强视觉层次与可读性，使课程作品更具完整产品感。

---

## 7. 关键算法与流程说明

虽然本系统不是复杂算法导向，但具有清晰的数据流程：

1. **添加流程**
   表单读取 -> 非空校验 -> SQL INSERT -> 刷新表格 -> 清空表单。

2. **修改流程**
   读取选中 ID -> 表单读取与校验 -> SQL UPDATE by ID -> 刷新表格。

3. **删除流程**
   读取选中 ID -> 确认弹窗 -> SQL DELETE by ID -> 刷新表格。

4. **查询流程**
   根据姓名/电话/类别构造 WHERE 条件 -> 参数绑定查询 -> 刷新显示。

5. **初始化流程**
   连接数据库 -> 建表 -> 判断空库 -> 从文本导入。

6. **导出流程**
   全量查询 -> 按行写入 `contacts.txt`。

这些流程体现了事件驱动系统中“用户操作触发槽函数、槽函数驱动状态变化”的典型模式。

---

## 8. 测试设计与结果分析

### 8.1 功能测试项

1. 添加联系人：
   - 正常输入可添加；
   - 姓名或电话为空时拦截；
   - 重复电话添加失败并提示。

2. 修改联系人：
   - 选中行后可按 ID 修改；
   - 未选中行时提示先选中。

3. 删除联系人：
   - 弹窗确认后删除；
   - 取消删除不影响数据。

4. 查询功能：
   - 按姓名/电话/类别均能返回对应结果；
   - 无结果时表格为空并显示状态。

5. 初始化与导出：
   - 空库可自动导入文本；
   - 同步文本可导出最新数据库数据。

### 8.2 环境限制说明

在当前容器环境中，因缺少 Qt6 开发包，无法完成完整编译与 GUI 运行测试；但构建脚本、代码结构、数据库逻辑均已按 Qt6 + MySQL 标准组织。实际在 Win11 + Qt6 + MySQL 环境可执行验证。

---

## 9. 存在问题与改进方向

### 9.1 当前不足

1. 数据访问逻辑仍在 `MainWindow` 中，业务与数据层耦合较高；
2. 错误信息可进一步细化（展示具体 SQL 错误）；
3. 尚未引入自动化测试框架；
4. 暂未实现分页、排序、多条件组合检索；
5. 配置文件明文密码存在安全隐患。

### 9.2 后续优化建议

1. 抽取 `ContactRepository` 与 `ContactService`，降低耦合；
2. 引入 `QSqlTableModel` 或 MVC 模型，减少手工刷新逻辑；
3. 增加输入正则校验（手机号、QQ）；
4. 增加操作日志表与时间戳字段；
5. 支持 CSV/Excel 导入导出；
6. 对数据库密码进行加密或系统凭据托管；
7. 引入单元测试与 UI 冒烟测试。

---

## 10. 总结

本项目围绕“通讯录管理”这一典型信息系统任务，完成了从课程基础要求到工程化实现的过渡：
- 在编程范式上，体现了 C++ 面向对象思想（抽象、封装、职责划分）；
- 在交互层面上，采用 Qt 构建了可操作、可视化、较美观的桌面界面；
- 在数据层面上，采用 MySQL 持久化并结合文本初始化/导出；
- 在工程组织上，采用 CMake + 配置文件方案，便于迁移部署。

该系统已具备课程设计作品应有的完整性：有需求、有结构、有实现、有数据、有交互、有反思。后续若进一步完成分层重构、测试补齐与安全增强，即可向“可长期维护的小型应用”演进。

---

## 附录 A：项目文件结构（简化）

- `CMakeLists.txt`：构建脚本
- `config.ini`：数据库连接配置
- `contacts.txt`：样例/同步文本
- `src/main.cpp`：程序入口
- `src/mainwindow.h`：主窗口类声明
- `src/mainwindow.cpp`：UI 与业务逻辑实现

## 附录 B：数据库初始化 SQL（概念示意）

```sql
CREATE TABLE IF NOT EXISTS contacts (
  id INT PRIMARY KEY AUTO_INCREMENT,
  name VARCHAR(50) NOT NULL,
  gender VARCHAR(10) NOT NULL,
  phone VARCHAR(20) NOT NULL UNIQUE,
  qq VARCHAR(20),
  category VARCHAR(50)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

